{
  "name": "medsol-vercel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create": "tfx extension create --manifest-globs vss-extension.json",
    "publish": "tfx extension publish --manifest-globals vss-extension.json --share-with ColinKrist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tfx-cli": "^0.11.0"
  }
}
